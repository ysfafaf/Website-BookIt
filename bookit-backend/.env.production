# Server
PORT=3001
NODE_ENV=production

# Database (Neon)
DATABASE_URL=postgresql://user:password@ep-xxxx.neon.tech/bookit?sslmode=require

# JWT
JWT_SECRET=use-a-long-random-string-minimum-32-chars-here
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS)
FRONTEND_URL=https://your-bookit-app.vercel.app